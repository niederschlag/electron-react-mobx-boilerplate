/* eslint-disable no-undef */
import React from 'react';
import { shallow, mount, render } from 'enzyme';

describe('example', () => {
    it('should be able to add 2 and 3', () => {
        expect(2 + 3).toBe(5);
    });
});
